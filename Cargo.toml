[package]
name = "chonky"
version = "0.1.0"
edition = "2018"
license = "Apache-2.0 WITH LLVM-exception"

[features]
default = ["std"]
metrics = [] # enable metrics support. minor performance impact probably.
std = []

[dependencies]
heapless = "0.7.4"